<% if user_signed_in? %>
    <li class="dropdown">
      <% if current_user.photo.file? %>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= image_tag current_user.photo.url(:circle), class: "img img-circle" %><b class="caret"></b></a>
      <% else %>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %><b class="caret"></b></a>
      <% end %>
      <ul class="dropdown-menu">
        <li><%= link_to 'Reviews', reviews_path %></li>
        <li><%= link_to 'Account', account_path %></li>
        <li><%= link_to 'Edit your infos', edit_user_registration_path %></li>
        <li><%= link_to 'Se déconnecter', destroy_user_session_path, method: 'delete', class: 'btn btn-link btn-publish' %></li>
      </ul>
    </li>
<% else %>
  <li><%= link_to 'Sign up', new_user_registration_path %></li>
  <li><%= link_to 'Sign in', new_user_session_path %></li>
<% end %>
